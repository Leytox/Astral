// Test stub for the generated Prisma client (src/generated/prisma/client.ts).
//
// The real module is emitted as ESM (it uses `import.meta`) and cannot be
// `require()`d by ts-jest's CommonJS transform. Every import of it in src/
// is used purely as a type, so empty classes are sufficient for unit tests.
// The actual client is never constructed because tests provide PrismaService
// via `{ provide: PrismaService, useValue: mock }`.

export class User {}
export class UserSession {}
export class Album {}
export class Song {}
export class Playlist {}
export class Genre {}
export class Role {}
export class PrismaClient {}
